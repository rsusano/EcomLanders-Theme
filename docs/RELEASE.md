# Release Process

1. Update CHANGELOG.md with version and changes
2. Tag the release: `git tag v1.0.0`
3. Push tags: `git push --tags`
4. Create a GitHub Release from the tag with notes from CHANGELOG
